# Engineering Workflow

- Status: Active
- Audience: Engineers
- Source of Truth: Yes
- Last Reviewed: 2026-04-13

## 목적

이 문서는 `piku-back` 저장소에서 커밋, 리뷰, 테스트 작업을 수행할 때 따라야 하는 기본 엔지니어링 워크플로를 정의한다.

## 커밋 규칙

### 타입

| 타입       | 설명                                                |
| ---------- | --------------------------------------------------- |
| `feat`     | 새로운 기능 추가                                    |
| `fix`      | 버그 수정                                           |
| `docs`     | 문서 수정                                           |
| `style`    | 코드 포맷팅, 세미콜론 누락 등 코드 변경이 없는 경우 |
| `refactor` | 코드 리팩토링                                       |
| `test`     | 테스트 코드, 리팩토링 테스트 코드 추가              |
| `chore`    | 빌드 업무 수정, 패키지 매니저 수정                  |

### 형식

```text
<타입>: <설명>
```

- 스코프(`()`)는 사용하지 않는다.
- 설명은 수행한 작업의 목적과 행위가 드러나야 한다.

### 필수 규칙

1. 파일명을 커밋 메시지에 나열하지 않는다.
2. `Phase 3`, `Phase 9` 같은 내부 작업 단위 명칭을 쓰지 않는다.
3. `주제: ~~` 형태의 모호한 표현을 쓰지 않는다.
4. `git add -A` 또는 `git add .`를 쓰지 않고, 작업한 대상 파일만 명시적으로 추가한다.

## 리뷰 규칙

### API 오류 응답 원칙

- 새로 추가되거나 변경되는 API 오류 응답은 RFC 9457 / Problem Details 원칙을 따라야 한다.
- 문자열 body를 직접 반환하는 오류 응답은 기본적으로 리뷰 지적 사항이다.
- 모듈 내부 전용 ad hoc error DTO를 새로 만드는 경우는 기본적으로 리뷰 지적 사항이다.
- 기존 Problem Details 전환 흐름과 무관한 새로운 오류 응답 형식을 추가하는 경우는 기본적으로 리뷰 지적 사항이다.

### 예외 허용 조건

- 아래 조건을 모두 만족할 때만 임시 예외를 허용한다.
- 하위 호환이 명확하게 필요하다.
- 왜 예외가 필요한지 설명할 수 있다.
- 최종적으로 Problem Details로 수렴하는 전환 경로가 문서나 계획에 남아 있다.

### 리뷰 시 확인 항목

- 상태 코드는 적절한가
- 오류 응답이 `ProblemDetail` 또는 이에 준하는 공용 생성 경로를 사용하는가
- `status`, `detail`이 일관되게 내려가는가
- `instance`, `type` 같은 필드를 쓰는 경우 프로젝트 규칙과 맞는가
- 기존 클라이언트 호환 때문에 예외를 두었다면 범위가 최소화되어 있는가
- DDD + 헥사고날 아키텍처 경계를 존중하는가
- 애플리케이션 계층이 웹, DB, 외부 인프라 세부사항에 직접 결합되지 않는가
- 포트와 어댑터 책임이 적절히 분리되어 있는가
- 변수명, 메서드명, DTO명, 포트명이 역할과 의미를 명확하게 드러내는가

## 테스트 규칙

### 테스트 구조

- `// given`, `// when`, `// then` 주석을 사용하지 않는다.
- 대신 빈 줄로 섹션을 구분한다.
- `@ExtendWith(MockitoExtension.class)` 사용을 기본으로 한다.
- `@Nested` + `@DisplayName`으로 메서드별 그룹핑을 선호한다.
- AssertJ (`assertThat`)를 사용한다.
- BDDMockito (`given`, `then`)를 사용한다.

### API 응답 테스트 규칙

- API 오류 응답을 변경하거나 추가할 때는 RFC 9457 / Problem Details 형식을 기본으로 검증한다.
- 최소 검증 항목은 `status`, `detail`이다.
- `instance`, `type` 같은 필드를 사용한다면 함께 검증한다.
- 하위 호환 때문에 임시 예외를 둘 경우, 왜 예외가 필요한지와 최종적으로 Problem Details로 수렴할 계획을 테스트 또는 문서에서 드러내야 한다.

## 브랜치 규칙

- Linear 추적 작업의 새 브랜치: `<type>/PIK-번호/<kebab-case-slug>`. 예: `feat/PIK-27/request-id-logging`.
- type은 변경 목적에 맞게 `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`를 사용한다.
- `dev`·`main` 이름은 유지하고 기존 브랜치를 자동 변경하거나 강제 푸시하지 않는다.
- 명시적으로 Linear 추적 없이 승인된 작업은 `<type>/<slug>`를 사용하며 이슈를 임의 생성하지 않는다.

## PR 식별과 Linear 자동 완료

- Linear 추적 PR 제목은 `[PIK-번호] 한국어 변경 요약`을 사용한다. 형식 예: `[PIK-27] 챕터형 회원가입 프론트엔드 디자인 및 구현`.
- 브랜치와 PR 제목의 ID는 실제 확인한 동일 담당 이슈를 가리킨다. PR 본문에는 Linear ID·URL·종료 참조를 넣지 않는다. 다른 담당 이슈를 함께 연결하지 않는다. ID 없는 기존 브랜치는 요청된 PR 제목으로 연결할 수 있지만 잘못된 ID가 있으면 먼저 해결한다.
- 필요한 검증·리뷰는 병합 전에 완료한다. Linear 기본 GitHub 연동의 대상 브랜치 `dev` 규칙에만 병합 시 Done을 설정한다. main·기본 병합 규칙은 No action으로 두고 설계 본문이 자동 댓글로 복제되지 않도록 Linkbacks를 끈다.
- 제출 후 Linear의 실제 PR 연동을 확인하고 완료 보고 전 병합과 이슈 상태를 다시 읽는다. 동일 담당 이슈의 필수 PR이 여러 개면 첫 병합 전에 전부 연결한다. 서로 다른 담당 이슈는 독립적으로 완료한다.
- 별도 동기화 스크립트·Actions workflow·API key secret·완료 JSON 댓글은 사용하지 않는다. 설정 안내 원본은 PM 저장소 `apfp77/pikume-pm`의 `docs/setup/linear-completion.md`다. 기존 CI workflow는 유지한다.
